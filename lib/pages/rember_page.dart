import 'package:flutter/material.dart';

class remberPge extends StatelessWidget {
  final Widget child;

  remberPge({Key key, this.child}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('remberpge'),
    );
  }
}